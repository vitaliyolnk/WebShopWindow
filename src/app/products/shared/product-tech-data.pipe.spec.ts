import { ProductTechDataPipe } from './product-tech-data.pipe';

describe('ProductTechDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductTechDataPipe();
    expect(pipe).toBeTruthy();
  });
});
